---
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'articles'>;
}

const { post } = Astro.props;
const base = import.meta.env.BASE_URL;
const { title, date, tags, summary } = post.data;

const formattedDate = new Intl.DateTimeFormat('en-GB', {
  day: 'numeric',
  month: 'long',
  year: 'numeric',
}).format(date);

const slug = post.slug;
---

<article class="post-card">
  <time datetime={date.toISOString()} class="post-date">{formattedDate}</time>

  <h2 class="post-title">
    <a href={`${base}articles/${slug}/`}>{title}</a>
  </h2>

  {summary && (
    <p class="post-summary">{summary}</p>
  )}

  {tags.length > 0 && (
    <div class="post-tags">
      {tags.slice(0, 3).map(tag => (
        <a href={`${base}tags/${tag}/`} class="tag">{tag}</a>
      ))}
    </div>
  )}

  <a href={`${base}articles/${slug}/`} class="read-more">
    Read
  </a>
</article>

<style>
  .post-card {
    padding: var(--space-xl) 0;
    border-bottom: 1px solid var(--color-border);
  }

  :global([data-theme="dark"]) .post-card {
    border-bottom-width: 2px;
  }

  .post-card:last-child {
    border-bottom: none;
  }

  .post-date {
    display: block;
    font-family: var(--font-heading);
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-text-muted);
    letter-spacing: 0.02em;
    margin-bottom: var(--space-sm);
  }

  .post-title {
    font-size: 1.375rem;
    font-weight: 700;
    margin-bottom: var(--space-sm);
    line-height: 1.3;
  }

  .post-title a {
    color: var(--color-heading);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .post-title a:hover {
    color: var(--color-accent);
  }

  .post-summary {
    color: var(--color-text-muted);
    font-size: 0.9375rem;
    line-height: 1.65;
    margin-bottom: var(--space-md);
    max-width: 60ch;
  }

  :global([data-theme="dark"]) .post-summary {
    color: var(--color-text);
  }

  .post-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    margin-bottom: var(--space-md);
  }
</style>
